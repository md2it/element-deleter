# Deletion

- Deletes the selected DOM element together with its child elements
- An iFrame is deleted entirely due to platform limitations
- Triggers a notification
- Multiple deletions:
   - After deleting an element, U can select and delete another element
   - Highlighting, deletion, and notification work the same way
